import { useState } from 'react';

function HomePage() {
    const [book, setBook] = useState(null);

    return (
        <main>
            <h1>Discover</h1>

            <p>teste</p>
        </main>
    );
}

export default HomePage;
